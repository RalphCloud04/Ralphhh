[package]
name = "crop_pay"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "25.3.0" }

[dev-dependencies]
soroban-sdk = { version = "25.3.0", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = "z"